<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlActivated">@android:color/white</item>
        <item name="colorControlHighlight">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>

    </style>

    <!-- Popup Menu Background Color styles -->
    <style name="MyPopupMenu"
           parent="@android:style/Widget.Holo.ListPopupWindow">
        <item name="android:popupBackground">@color/fragment_background</item>
    </style>
    <!-- Popup Menu Text Color styles -->
    <style name="TextAppearance">
        <item name="android:textColor">@color/main_color</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base"/>

    <style name="CustomDigitsTheme" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/main_color</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColorLink">@color/main_color</item>
        <item name="colorAccent">@color/main_color</item>
    </style>

    <style name="MaterialSpinner" parent="Widget.AppCompat.Light.Spinner.DropDown.ActionBar">
        <item name="android:background">?android:selectableItemBackground</item>
        <item name="android:dropDownSelector">?android:selectableItemBackground</item>
        <item name="android:divider">@null</item>
        <item name="android:overlapAnchor">true</item>
        <item name="overlapAnchor">true</item>
        <item name="android:popupBackground">#EEEEEE</item>
    </style>

    <!-- APP Theme which extends Theme App Compact -->
    <style name="CustomActionBarTheme" parent="@style/Theme.MyTheme">
        <item name="android:actionOverflowButtonStyle">@style/OverflowButton</item>
        <item name="actionOverflowButtonStyle">@style/OverflowButton</item>

        <item name="android:dropDownListViewStyle">@style/PopupMenuListView</item>
        <item name="dropDownListViewStyle">@style/PopupMenuListView</item>

        <item name="actionBarDivider">@null</item>

        <!-- OverFlow Menu Text Color -->
        <item name="android:textColor">@color/main_color</item>
    </style>

    <!-- Set Custom Icon for Overflow Button in ActionBar -->
    <style name="OverflowButton" parent="@style/Widget.AppCompat.ActionButton.Overflow">
    </style>

    <!-- OverFlow menu Styles -->
    <style name="PopupMenuListView" parent="@style/Widget.AppCompat.Light.ListView.DropDown">
        <item name="android:divider">#dee0e2</item>
        <item name="android:dividerHeight">2dp</item>
        <item name="android:listSelector">@drawable/menu_item_background_color</item>
        <item name="android:background">@color/fragment_background</item>
    </style>


    <style name="Theme.MyTheme" parent="Theme.AppCompat.Light">


        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/main_color</item>

        <!-- colorPrimaryDark is used to color the status bar -->
        <item name="colorPrimaryDark">@color/main_color_dark</item>

        <!--
             colorAccent is used as the default value for colorControlActivated
             which is used to color widgets
        -->
        <item name="colorAccent">@color/main_color</item>

        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight & colorSwitchThumbNormal. -->
        <item name="android:textColorPrimary">@color/main_color</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColorLink">@color/main_color</item>
    </style>


    <style name="RelishDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="background">@android:color/transparent</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>


    <!-- ChangeLog/About dialog theme -->
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/main_color</item>
        <item name="android:textColorPrimary">#000</item>
        <item name="android:windowTitleStyle">@style/TitleDialogTextStyle</item>
    </style>

    <style name="TitleDialogTextStyle" parent="Base.DialogWindowTitle.AppCompat">
        <item name="android:textColor">@color/main_color</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

</resources>
